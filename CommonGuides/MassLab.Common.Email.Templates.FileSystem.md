# MassLab.Common.Email.Templates.FileSystem

Place templates below `Templates/{key}` with `subject.hbs`, `body.html.hbs`, and optional `body.text.hbs` files. Bindings use Handlebars and unresolved bindings fail before an email is sent.
