# MassLab.Common.Email.Smtp

Configure `Email:Smtp` and register with `AddSmtpEmail()`. SMTP reports acceptance by the relay; delivery lifecycle webhooks are provider-specific and are not available through SMTP.
